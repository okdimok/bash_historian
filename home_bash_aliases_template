# This file is prepended in the __install_bash_aliases function
if [ -f $BASH_HISTORIAN_ALIASES ]; then
    . $BASH_HISTORIAN_ALIASES
else
	echo "The ~/.bash_aliases points to bash_historian, but it cannot be found"
fi
